module.exports = {
  // We don't use the built-in locale detection since we're using query parameters
  i18n: {
    defaultLocale: 'de',
    locales: ['de', 'ar'],
  },
  defaultNS: 'common',
  localePath: './public/locales',
}